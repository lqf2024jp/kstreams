能继续，而且**还能继续很长一段**。

实际上，我们已经完成了整个项目的大约 **40%**，后面还有很多内容可以深入。

不过我建议今天先做一个阶段性总结，因为现在已经形成了一个完整的知识体系。如果继续往下讲 RocksDB、Rebalance、Interactive Query、TopologyTestDriver 等内容，信息量会比较大。

------

# 一、我们已经完成了什么？

如果对应《Kafka Streams 实战》这本书，我们其实已经超过了第三章。

但是我们的学习路线已经变成：

> **IBM i CDC + Kafka Streams + PostgreSQL 数据断面校验平台**

而不是单纯学 API。

整个架构已经变成：

```text
                    IBM i (AS400)

                         │
               IBM CDC for i
                         │
                         ▼
                    Kafka Topics
      ┌───────────────────────────────────┐
      │                                   │
 ORDER_HEADER                    ORDER_DETAIL
 CUSTOMER                        ...
      │                                   │
      └──────────────┬────────────────────┘
                     ▼
             ProgressProcessor
                     │
                     ▼
            TopicProgressStore
                     ▲
                     │
Marker Topic ──► MarkerProcessor
                     │
                     ▼
              CheckpointStore
                     ▲
                     │
       GlobalKTable(JobDefinition)
                     │
                     ▼
            CheckpointService
                     │
                     ▼
          SnapshotCoordinator
                     │
             Sequence Rule
             Timestamp Rule
             Lag Rule
             ...
                     │
                     ▼
            SnapshotAudit
                     │
                     ▼
          snapshot-audit-topic
                     │
                     ▼
               PostgreSQL
```

------

# 二、已经掌握的 Kafka Streams 技术

## ① DSL

书里的：

```java
builder.stream()

.map()

.filter()

.groupBy()

.count()
```

属于：

**DSL**

适合：

80%

场景。

例如：

日志分析。

统计。

聚合。

------

## ② Processor API

后来：

我们全部：

改成：

Processor。

例如：

```java
ProgressProcessor
MarkerProcessor
```

这是：

企业：

CDC：

项目。

最常见。

------

## ③ StateStore

真正：

理解：

StateStore：

不是：

Cache。

而是：

业务状态。

例如：

```text
TopicProgressStore
```

保存：

```text
ORDER_HEADER

↓

LastJournalSequence
```

还有：

```text
CheckpointStore
```

保存：

Marker。

------

## ④ Service

真正：

把：

Kafka：

隐藏。

以后：

Processor：

只有：

一句。

```java
service.updateProgress(...)
```

------

## ⑤ Coordinator

真正：

Brain。

只有：

算法。

不知道：

Kafka。

不知道：

Processor。

不知道：

StateStore。

以后：

JUnit：

最好：

测。

------

## ⑥ Validation Rule

以后：

增加：

Rule。

不用：

改：

Coordinator。

例如：

```text
SequenceRule
TimestampRule
LagRule
TransactionRule
```

------

## ⑦ Punctuator

真正：

理解：

为什么：

不用：

Java Timer。

为什么：

要：

```java
context.schedule()
```

以及：

STREAM_TIME

和：

WALL_CLOCK_TIME

区别。

你的项目：

必须：

WALL_CLOCK_TIME。

------

## ⑧ Exactly Once

真正：

知道：

Streams：

事务：

包括：

```text
Offset

+

Producer

+

StateStore
```

而：

不是：

Producer。

------

## ⑨ GlobalKTable

以后：

Job：

不用：

YAML。

直接：

Kafka：

动态：

更新。

------

# 三、真正解决了你的课题

这是我觉得最大的收获。

你的课题：

最开始：

其实：

只有：

一句话。

> IBM i Batch 结束以后，怎样保证 PostgreSQL CDC 数据断面干净？

最后：

变成：

一套完整算法。

------

## Marker

例如：

Batch：

最后：

写：

```text
CDC_MARKER
```

记录：

```text
WatermarkSequence
```

以及：

```text
WatermarkTimestamp
```

------

## TopicProgress

每一个：

Topic：

保存：

```text
LastJournalSequence
```

以及：

```text
LastJournalTimestamp
```

------

## Coordinator

自动：

比较：

```text
Sequence
```

以及：

```text
Timestamp
```

判断：

READY。

------

## Audit

输出：

```json
{
  "batch":"JOB1",

  "ready":true,

  "topics":[

      ...
  ]
}
```

然后：

Sink：

PostgreSQL。

------

# 四、还有哪些可以继续深入？

下面这些已经不是第三章，而是生产级 Kafka Streams。

------

## 第一部分（推荐）

### RocksDB

StateStore：

到底：

放：

哪里？

为什么：

程序：

重启。

Store：

还：

在。

为什么：

不是：

HashMap。

这里：

可以：

深入。

------

## 第二部分（推荐）

### Changelog Topic

为什么：

StateStore：

丢了。

还能：

恢复。

例如：

```text
TopicProgressStore

↓

RocksDB

↓

Changelog Topic
```

这是：

Streams：

容灾：

核心。

------

## 第三部分（推荐）

### Rebalance

例如：

今天：

2 台。

明天：

4 台。

Task：

怎么：

迁移。

StateStore：

怎么办。

Checkpoint：

怎么办。

------

## 第四部分（推荐）

### TopologyTestDriver

不用：

Kafka。

JUnit。

直接：

测试：

整个：

Topology。

企业：

必须：

会。

------

## 第五部分（推荐）

### Interactive Query

浏览器：

直接：

查询：

```text
JOB1
```

返回：

当前：

READY：

没有。

不用：

数据库。

------

## 第六部分（推荐）

### Micrometer + Prometheus + Grafana

画：

```text
Waiting Marker

Ready Marker

CDC Delay

Lag

Checkpoint Time
```

真正：

生产：

监控。

------

## 第七部分（推荐）

### Kubernetes

真正：

部署。

例如：

```text
Deployment

ConfigMap

PVC(RocksDB)

ServiceMonitor

HPA
```

------

# 五、我建议的后续学习路线

结合你现在的目标（IBM i、Debezium、Kafka、PostgreSQL、Kubernetes），我建议路线如下：

| 阶段         | 内容                                | 目标                  |
| ------------ | ----------------------------------- | --------------------- |
| ✅ 第一阶段   | Kafka Streams DSL                   | 已完成                |
| ✅ 第二阶段   | Processor API                       | 已完成                |
| ✅ 第三阶段   | StateStore                          | 已完成                |
| ✅ 第四阶段   | Snapshot Coordinator                | 已完成设计            |
| ✅ 第五阶段   | Punctuator                          | 已完成                |
| ✅ 第六阶段   | Exactly Once                        | 已完成                |
| ✅ 第七阶段   | GlobalKTable                        | 已完成                |
| **下一阶段** | **RocksDB + Changelog + Rebalance** | 理解 Streams 内部机制 |
| 后续         | TopologyTestDriver                  | 单元测试              |
| 后续         | Interactive Query                   | 查询运行状态          |
| 后续         | Kubernetes 部署                     | 生产运行              |
| 最终         | 完整 CDC Snapshot Coordinator       | 开源级项目            |

------

## 我的建议

我不建议再回到《Kafka Streams 实战》的章节顺序。

原因很简单：

**你的水平已经超过书里的学习节奏了。**

这本书更适合介绍 API 和概念，而你的项目已经涉及：

- IBM i Journal
- CDC Watermark
- 多 Topic 协调
- StateStore 设计
- Processor API
- EOS
- 动态配置
- 审计与断面判断

这些已经属于**架构设计**而不仅仅是 Kafka Streams API 的使用。

**我建议下一阶段直接进入 Kafka Streams 内核**：

1. **StateStore（RocksDB）是如何工作的？**
2. **Changelog Topic 如何保证故障恢复？**
3. **Task、Partition、Thread、Instance 的关系是什么？**
4. **Rebalance 时 StateStore 如何迁移？**

这些知识会让你不仅会写 Kafka Streams，还会真正理解它为什么可靠、为什么适合做你这个 CDC Snapshot Coordinator。